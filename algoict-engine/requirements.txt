# AlgoICT Engine — Python Dependencies
# Install: pip install -r requirements.txt

# Data & Analysis
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
TA-Lib>=0.4.28

# Brokers
alpaca-py>=0.21.0
signalrcore>=0.9.5
websockets>=12.0
httpx>=0.27.0
# aiohttp is used directly by brokers/topstepx.py (REST client + auth).
# Locally it was pulled in as a transitive dep of alpaca-py/supabase, but
# CI clean-installs broke when a fresh resolver didn't include it
# (2026-04-24 CI failure on commit a33cc12). Pin explicitly.
aiohttp>=3.9.0

# Database
supabase>=2.3.0

# AI Agents
anthropic>=0.40.0

# Alerts
python-telegram-bot>=21.0

# Market Data
yfinance>=0.2.36

# Utilities
python-dotenv>=1.0.1
pydantic>=2.6.0
# Used by sentiment + heartbeat modules. Locally pulled in transitively
# by pandas + supabase respectively, but pinned explicitly so a clean
# CI install doesn't fail (2026-04-24 audit).
python-dateutil>=2.9.0
requests>=2.31.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
